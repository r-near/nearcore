[package]
name = "mldsa-diff116"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
aws-lc-rs = { version = "=1.16.2", features = ["unstable"] }
near-mldsa-native-sys = { path = "../../core/mldsa-native-sys" }

[profile.release]
lto = "fat"
codegen-units = 1

[workspace]
